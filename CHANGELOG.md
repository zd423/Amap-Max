# 更新日志

本文档用于记录 AMap Companion 的更新日志。

## 2026-08-08 集成关闭 AEB 功能 + 极狐设置二级菜单（v1.8.16 / 1843）

将独立工具 App `AEB_Disabler` 的关闭 AEB 能力集成进 AMap Max：

- **manifest**：加 `sharedUserId="android.uid.system"`（系统 UID）+ 3 个车辆权限（`WRITE_VEHICLE_PROPERTIES` / `MODIFY_VEHICLE_PROPERTIES` / `READ_VEHICLE_PROPERTIES`）；注册 `AebDisableService`（exported，intent-filter `com.autonavi.companion.START_AEB_DISABLE`）与 `ControlReceiver`（adb/脚本触发通道 `com.autonavi.companion.action.DISABLE_AEB`）。
- **AebDisableService**：从 `com.aeb.disabler` 移植，双通道写 VHAL 关 AEB（优先反射 `BcmManager.setIntTransDataMessage("DeviceInfo", 0x21400b35, 0, 1)`，兜底 `CarPropertyManager.setIntProperty`），回读 `0x21400b36` 确认，状态写 prefs；写 VHAL 需要系统签名/系统 UID。
- **android.car 编译期占位桩**：`android.car.Car` / `CarPropertyManager` 复制进源码树（真机由 AAOS boot classpath 真实实现替换），R8 加 keep 防改名。
- **UI**：一级菜单「极狐转向」→「极狐设置」；点开右侧二级菜单页——`自动关闭 AEB`（直接开关）+ `极狐转向`（原设置入口）。
- **独立 AEB 开关**：默认关闭（用户手动打开后才生效）；开关开 → 立即执行一次 + 以后每次开机/亮屏（BootReceiver）自动重关（车机重启后 AEB 自动恢复开启）；开关关 → 不执行。
- **只写一次**：`MAX_RETRY=0`，双通道任一成功即算成功（静止状态回读不可信，不回读重试），失败立即停（与原工程 `-Writes 1` 产物一致）。
- **build.ps1 修复**：源码路径含空格（`D:\OpenCode\AMap Max`）导致 javac/R8 失败 → 文件列表逐个加引号。
- **验证**：模拟器 1300×900 全链路通过（系统 UID 授予、二级菜单、开关默认开/可切换落盘、服务执行写状态、无 crash）；AEB 实际写入需实车验证（模拟器非 AAOS）。
- **⚠️ 实车注意**：加系统 UID 后须**先卸载旧版**（否则 `INSTALL_FAILED_UID_CHANGED`），旧 prefs 配置会丢。

## 2026-08-04 设置页全量夜间适配（v1.8.15 / 1842）

1841 只修了百分比输入框，继续扫查发现设置页还有 **3 处固定浅色**未适配夜间：

- **转向形状/效果值标签**（极狐转向 tab）：`#EFF6FF` 浅蓝底 + `#BFDBFE` 浅蓝边 + `#2563EB` 蓝字 → 夜间改为 `#1E3A5F` 深蓝底 + `#3B82F6` 蓝边 + `#60A5FA` 亮蓝字
- **应用选择标签**（通用设置 app 列表）：7 种语义标签色（高德包名/地图匹配/全部列表/系统应用/用户应用/可打开/无桌面图标）全部补夜间深色变体
- 所有颜色均通过 `isDarkMode()` 联动，白天保持原样不变

## 2026-08-04 修复设置页输入框夜间仍为白底（v1.8.14 / 1841）

模拟器实测发现：App 自身设置页（悬浮窗显示位置）的百分比输入框在夜间模式下仍是刺眼白底——其背景用了固定浅灰 drawable `R.drawable.ios_edit_bg`（`#F2F2F7`），不随夜间变化；而同页的卡片/文字/开关都靠 `isDarkMode()` 动态适配，唯独输入框漏了。

- **修复**：`MainActivity.overlayScaleRow` 去掉固定 `setBackgroundResource(R.drawable.ios_edit_bg)`，改为根据 `isDarkMode()` 动态生成圆角背景——夜间 `#2D2D2D` 深灰底 + 白字、白天 `#F2F2F7` 浅灰底，并补回原 drawable 的 `10/6dp` padding。
- **范围**：仅 `overlayScaleRow` 主屏/副屏悬浮窗缩放输入框（设置页唯一一处 EditText）；`ios_edit_bg.xml` 已不再被引用（保留未删，属改动产生的孤儿资源）。

## 2026-08-04 修复夜间模式悬浮窗无变暗效果（v1.8.13 / 1840）

实车反馈「夜间模式下悬浮窗实际还是白色的，夜间效果没生效」。根因：悬浮窗背景透明度硬编码 `opacity=0`（全透明），`nightPaletteBg` 的夜间纯黑背景色被 `withAlpha(_, 0)` 完全抹掉；文字白天近白（0xFFE8EAED）/夜间纯白（0xFFFFFFFF）又几乎无差别 → 夜间模式视觉上零变化（透明底 + 白字）。

- **修复**：`OverlayService` 三处背景（`buildPanel` / `createMainPanelBackground` / `createClusterPanelBackground`）`opacity = 0` → `isNightMode() ? 85 : 0` —— 夜间模式悬浮窗呈现 **85% 深色底（0xFF0D0D0D 纯黑 @ 85%）**，配合原有夜间白字 + 红绿灯 ×0.70 暗化，夜间变暗效果肉眼可见；**白天保持透明悬浮不变**（不影响既有观感）。
- **生效链路**：文字模式切换（跟随系统/强制白天/强制夜间）→ `ACTION_OVERLAY_STYLE_CHANGED` → 悬浮窗重建 → 重读夜间判断 → 新背景立即生效；系统 uiMode 变化 → `ACTION_CONFIGURATION_CHANGED` → 同样重建。
- **提醒**：实车若车机系统深色模式不自动切换，请在 App「悬浮窗设置 → 文字模式」选**强制夜间**以保证生效。

## 2026-08-04 修复转向方向误判闪现：打左闪右 / 打右闪左（v1.8.12 / 1839）

实车反馈「打左转 HUD 右转会闪现（随后灭）、打右闪左」。根因在 `AdayoTurnSignalMonitor` 的方向判定，与渲染无关：

- **`directionFor` 负数误判为亮（主因）**：原 `leftStatus != 0` 把负数也当"亮"。实车 commservice 在灯未激活时输出 `status:-1` 占位值 → 打左转瞬间配对 `(1,-1)` → 误判 `HAZARD`（左右同时亮）→ 右侧闪现；随后右灯真实状态帧到达 → `(1,0)` 纠正为 LEFT → 右侧灭。打右同理闪左。**改为 `status > 0` 才算亮**（负数=未激活，0=灭）。
- **非 OFF 方向切换加确认窗口（辅因：快速换向乱序）**：左右 lamp 日志行各取"各自最新值"配对，快速换向（RIGHT→LEFT）瞬间可能配对出瞬时错误方向（如 `(1,1)` 误报 HAZARD）。新增 80ms 确认窗口：新方向需在窗口内被后续配对再次确认才发布，窗口内配对又变则跟随最新方向；**OFF 不设防抖**（熄灯立即发布，避免依赖后续日志行确认导致 HUD 卡亮）；同方向 raw 微调不设防抖（转向灯正常闪烁节拍不受影响）。

效果：打方向不再闪对面，方向切换干净利落；模拟器 log 注入对照验证（1838 vs 1839）确认修复生效。

## 2026-08-04 设置弹窗高度贴合内容（v1.8.11 / 1838）

修复实车反馈「极狐转向设置弹窗太高、底部一大片空白」：`TurnSignalSettingsSheet` 弹窗原为固定 **85% 屏高**（1300×900 屏 = 765px），内部 ScrollView 又设 `weight=1f` + `fillViewport=true`，即使实际内容仅 ~500px 也会撑满整个窗口。

- **窗口高度 WRAP_CONTENT**：Dialog 窗口从 `setLayout(w, 85%屏高)` 改为 `setLayout(w, WRAP_CONTENT)`，弹窗高度完全由内容决定。
- **ScrollView 去除 weight 撑满**：ScrollView 的 LayoutParams 从 `(MATCH_PARENT, 0, weight=1f)` 改为 `(MATCH_PARENT, WRAP_CONTENT)`，不再吃掉标题栏下方的全部剩余空间。
- 效果：弹窗从 ~765px 高收窄到紧贴内容的 ~520px（具体值随 density 浮动），底部空白彻底消除。

## 2026-08-04 转向弹窗高度贴合箭头内容（v1.8.10 / 1837）

修复实车反馈「极狐转向弹窗太高、底下一大片空白」：转向悬浮窗（副屏 HUD）原为 `MATCH_PARENT`×`MATCH_PARENT` 全屏窗口（720 高），箭头按 topFactor 画在屏高 40% 处，下方 432px 全是空白窗口。

- **窗口高度贴合箭头内容**：`TurnSignalOverlayView.onMeasure` 返回内容高度（`2×halfHeight + 8dp` 留白），`TurnSignalController.attachToHost` 与 `OverlayService.attachClusterTurnSignalWindow` 的高度参数 `MATCH_PARENT → WRAP_CONTENT`。模拟器实测窗口高度从 720px 收窄到 115px（= 2×19×2.5 + 8×2.5，与公式精确一致）。
- **窗口 y 对准 topFactor**：新增 `OverlayService.positionClusterTurnSignalWindow()` —— 窗口高度贴合后，把窗口 y 定位到 `topFactor×屏高 − 内容高/2`（箭头中心 = topFactor，默认 40%），clamp 到设置范围（8..92）；挂载后 `post()` + layout listener 双保险确保首次 layout 完成后定位，`refreshTurnSignalOverlay` 末尾补重定位（尺寸/topFactor 变更后即时生效）。
- **箭头垂直居中**：`onDraw` 的 `centerY` 由 `topFactor*height` 改为 `height/2f`（窗口已贴合，垂直位置由宿主窗口 y 决定）。
- **尺寸变更即时生效**：`refreshPreferences()` 补 `requestLayout()`（原先只 `invalidate()`，onMeasure 不会重跑，尺寸滑块调整后内容高度不刷新）。
- 模拟器回归：LEFT/RIGHT/HAZARD 三向箭头均渲染在副屏 40% 垂直位置（窗口 frame=[0,231][1280,346]，箭头中心 288.5 ≈ 40%×720），底部空白消除。

## 2026-08-04 Karpathy 四原则修复（v1.8.9 / 1836）

基于全工程 Karpathy 审查报告（`AMap_Karpathy_Review_20260804.md`）批量修复，按「编码前思考 / 简洁优先 / 精准修改 / 目标驱动执行」四原则执行，7 批次共删减死代码约 160 行：

- **死代码清理（D 组）**：`MainActivity` 删除 7 个未用 URL 常量、`ArrowDrawable` 死类（30 行）、`chooseDownloadSource()` 与孤儿 `openUrl()`；`OverlayService` 删除 `activateClusterBridge` 空壳及调用点、`applyTextPalette`/`syncModeVisibility` 空体及调用点、`preferLightState`/`mergeTrafficLights`/`booleanValue`/`doubleValue`/`clusterSp` 死方法、`lightPill` 4 个死重载；`TurnSignalController` 删除 `renderedDirection` 字段及赋值；`AppPrefs` 删除 `isCardLikeOverlayUiEnabled`/`isNewOverlayUiEnabled`/`isDynamicIsland*` 系列死方法；`OverlayUiStyles` 删除恒 true 的 `isCardLike()` 与 `Style.cardLike` 字段。
- **空壳方法（E 组）**：随 D 组一并清除（`activateClusterBridge` 乱码注释空壳、`applyTextPalette`/`syncModeVisibility` 空体等）。
- **魔法值（M 组）**：`TurnSignalOverlayView` 5 处、`TurnSignalSettingsSheet` 2 处 `shape==4` 改用 `AppPrefs.TURN_SHAPE_STATIC` 具名常量；`TrafficLightParser` 4 处 `999` 改用 `DIR_UNSPECIFIED`。
- **反射去重（R9）**：`MainActivity` 3 处 `startForegroundService`、`OverlayService` 的 `ensureNotificationChannel`/`createNotificationBuilderWithChannel`/`canUseOverlayWindowType` 反射调用全部改为直接调用（compileSdk=android-31 满足 API，仅保留 minSdk=23 的 SDK_INT 分支）。
- **onDraw 对称重构（R1）**：`TurnSignalOverlayView.onDraw` 左右两侧 5 分支对称复制合并为带 `rightMirrored` 的单分支分发（含 `staticShape` 奇偶偏移修正）；`renderColor` 死字段删除，14 处引用统一为 `color`。
- **PALETTE 结构简化**：`int[2][1]` → `int[2]`，调用点索引同步修正。
- **静默失败补日志（S1/S2）**：`AdayoTurnSignalMonitor` 两处监听器回调 `catch (Throwable ignored){}` 补充 `Log.w`（含异常堆栈），实车监听器异常不再无从排查。
- **注释修正**：`safe_left` 默认值矛盾注释（默认 30 非 0）、`MAX_TURN_SIGNAL_SHAPE` 增补 STATIC 说明。
- 保留决策：`AppPrefs` 两个 `clamp` 变体边界语义不同（对称/偏置），具名封装更清晰，不合并避免过度抽象。

## 2026-08-04 代码质量优化（v1.8.8 / 1835）

- **优化（P3-3）**：提取 `paintClassicArrowBody()` 公共方法 —— 一体成型燕尾箭头本体绘制（Path 构建 + 柔和 glow + 核心实色）由 `drawClassicArrow()` / `drawStaticArrow()` 共用，消除约 30 行重复代码。
- **优化（P3-1）**：`drawStaticArrow()` 去掉恒 1 次的 `for (i < 1)` 循环，直接取 `firstCenter`；`spacing`/`halfWidth`/`halfHeight` 未用参数保留签名并注释说明（便于未来恢复双箭头）。
- 纯维护性重构，渲染行为零变化（箭头像素与 1834 逐帧一致）。

## 2026-08-04 代码审查修复（v1.8.7 / 1834）

- **修复（P2-1）**：`findClusterDisplay()` 在 prefs 指定 `cluster_display_id` 找不到对应 Display 时，补充警告日志（含目标 id 与当前 Display 数量），避免虚拟副屏重建后挂载静默失败无从排查。
  - 配套修正：proguard 原 `-assumenosideeffects` 会把 `Log.w/e` 一并裁剪导致该日志失效 → 改为仅裁剪 `d/i/v`，保留警告/错误日志用于实车排查（dex +1.3KB）。
- **修复（P2-2）**：静态箭头（形状 4）下「动画特效」不生效但无可视反馈 → 设置页（Sheet 弹窗 + 主界面卡片）检测到形状为静态箭头时，效果值追加「（静态箭头无效果）」并置灰；切换形状后提示自动恢复。
- **优化（P3-2）**：修正过时注释 —— `AppPrefs.DEFAULT_TURN_SIGNAL_SHAPE` 范围注释 `0..3` → `0..4`，`TurnSignalOverlayView.shape` 字段注释补充形状 4（静态箭头）。

## 2026-08-04 静态箭头改单箭头（v1.8.6 / 1833）

- 「静态箭头」每侧由 2 个箭头改为 **1 个箭头**（用户实车确认）：更贴近真车转向灯直觉，干净利落。
- 仅静态箭头（形状 4）改为单箭头，传统箭头（形状 3）仍为双箭头；造型保持一体成型燕尾样式不变。

## 2026-08-04 新增「静态箭头」：无动态实车模式（v1.8.5 / 1832）

- 新增第 5 种箭头形状「静态箭头」：复用一体成型燕尾箭头造型（传统箭头 B 版），但**完全无动态** —— 恒定亮度点亮、无流动/闪烁/呼吸/白闪，按方向静止显示。
- 实车适配：静态箭头模式下动画循环整体停掉（`shape != 4` 条件），不启动 Handler 重绘，避免实车长时间点亮时白耗 CPU。
- 形状选择从 4 种扩展为 5 种（V形箭头 / 流水灯带 / 实心箭头 / 传统箭头 / 静态箭头），设置页与主设置页同步，摘要卡自动适配。
- 形状上限常量 `MAX_TURN_SIGNAL_SHAPE` 提升至 4，旧存档自动 clamp。

## 2026-08-04 传统箭头改版：一体成型燕尾箭头（v1.8.4 / 1831）

- 「传统箭头」从「三角头 + 短杆两段拼接」改为「一体成型燕尾箭头」：三角头尾部带燕尾凹槽，杆直接从凹槽内长出，整条轮廓为单一连续 Path，无拼接缝隙，为经典交通指示牌/转向指示灯箭头造型。
- 尺寸微调：头长 26dp / 头高 36dp / 杆长 16dp / 杆宽 13dp（旧版 24/34/14/12），整体更饱满协调。
- 发光质感柔和化：外层 glow 半径 12→14dp、透明度 0.22→0.18，核心光晕半径 4→6dp，边缘不再锐利，更贴近灯珠光晕质感。

## 2026-08-04 极狐转向新颜色与形状（iOS 亮绿 + 传统箭头）

- 新增 iOS 亮绿（#34C759）预设颜色：转向箭头颜色选择从 6 色扩展为 7 色，新增色块与现有荧光青绿、青色、琥珀黄、iOS 红、白、淡紫并排显示，选择弹窗色块行完整展示不裁切。
- 新增「传统箭头」形状：箭头形状从 3 种扩展为 4 种（V形箭头 / 流水灯带 / 实心箭头 / 传统箭头），传统箭头采用经典实心转向指示灯图形（实心三角头 + 短杆），FILL 填充 + 双层 glow 发光 + 白闪高亮，画 2 个更醒目。
- 形状上限常量 `MAX_TURN_SIGNAL_SHAPE` 同步提升至 3，旧存档值自动 clamp 适配，无需用户手动迁移。
- 主设置页极狐转向摘要卡（`buildTurnSummary()`）自动适配新增形状名称，切换后即时刷新显示。
- 修复验证链路：主屏转向预览关闭后，通过 `settings put global overlay_display_devices` 创建 720x1280 虚拟副屏，转向窗口挂载到虚拟副屏后截图验证渲染（模拟器不直接支持副屏 screencap，改用主屏「叠加视图」预览窗抓帧分析确认箭头形状与颜色）。

## 2026-06-07 低透明度可读性优化

- 所有悬浮窗文字新增自动对比描边：根据当前文字颜色自动选择黑色或白色轮廓，改善低背景透明度、浅色地图和复杂车机桌面上文字融入背景的问题。
- 主屏与仪表副屏使用相同的文字描边逻辑，切换自动文字颜色或不同 UI 样式后会重新应用轮廓。
- 无蓝色背景的车道图标新增黑色轮廓，包括高德车道位图和程序绘制的兜底箭头；带蓝色背景的车道栏保持原有样式，避免重复描边。
- 继续优化无法识别的特殊车道兜底图标尺寸与资源体积。

## 2026-06-06 特殊车道兼容与桌面启动直达

- 新增无法识别的特殊车道兜底图标：高德广播中的未知或扩展车道编码不再显示为空白，统一使用特殊车道提示图。
- 特殊车道提示图单独缩小至普通车道图标的约 85%，改善与常规车道并排显示时的视觉比例，普通车道尺寸保持不变。
- 新增“桌面启动时直接进入目标应用”选项：开启后，从桌面点击 AMap Companion 会直接打开用户选择的目标应用。
- 桌面直达不会影响伴侣设置入口；点击主屏悬浮窗仍会进入 AMap Companion 设置页面。
- 桌面直达时会按现有主屏、副屏和广播自动显示选项恢复伴侣服务，不改变用户已经选择的悬浮窗显示策略。
- 桌面直达新增快速返回设置规则：首次点击桌面图标进入目标应用，30 秒内再次点击则进入伴侣主界面；后续点击会重新开始一轮判断。
- 修复主屏悬浮窗首次显示后 4 秒内不可触摸的问题，现在窗口出现后即可单击进入伴侣设置；副屏启动保护逻辑保持不变。
- “选择目标应用”改为先显示弹窗、后台扫描应用列表，避免枚举应用和排序时阻塞主界面；应用图标增加缓存，改善列表滚动流畅度。

## 2026-06-06 广播解析重构、主副屏稳定性与卡片 UI 完善

- 重构高德广播处理架构：红绿灯、车道、服务区、偏好设置、日志收集、Markdown 渲染和 UI 样式注册拆分为独立模块，统一数据解析入口，降低不同 UI 重复解析和后续扩展新样式的成本。
- 经典 UI、卡片 UI、灵动岛、灵动岛（测试）和新 UI 改为独立布局构建；保留经典 UI 原有车道样式，修复重构期间不同 UI 车道布局互相覆盖、导航道路信息缺失等问题。
- 修复启用副屏镜像后切换 UI 可能闪退或显示旧布局的问题：切换样式时会先完整销毁旧仪表镜像，再按新样式重建，主屏与副屏显示内容和格式保持一致。
- 修复反复点击“关闭伴侣服务/启动伴侣服务”后出现多层悬浮窗残留的问题；关闭服务现在会同步移除主屏和副屏窗口，并取消初始化、副屏重试、超速闪烁等全部延迟任务。
- 统一无导航数据时的初始状态，各 UI 显示“待接收导航/巡航信息”；高德退出或停止广播后会清理道路、方向、车道、红绿灯和电子监控旧数据，不再长期残留上一段导航内容。
- 卡片 UI 继续优化：修复导航信息行重叠和副屏底部空白行；巡航顶部调整为“方向、道路名称、电子监控”，方向与道路名紧贴，道路名与电子监控保留适当间距。
- 修复限速和限速拍照图标显示：三位数限速值可完整容纳在圆形标识中；限速拍照使用纯限速圆显示数值，不再与摄像头图案重叠。
- 灵动岛 ETA 信息列加宽，“余 xxx km”固定单行显示；新 UI 在相同悬浮窗比例下适当缩小，改善与其他样式面积差异过大的问题。
- 经典 UI 新增高速服务区信息开关，并支持同一广播中的多个服务区；服务区名称与距离按行显示，广播清空或导航退出后同步清除残留。
- 调整超速边框策略：普通超速可选蓝绿色 5 秒闪烁/20 秒停留，超速 10% 可选黄色 5 秒闪烁/10 秒停留，超速 20% 强制红色持续提醒。
- 删除仅用于开发过程的 `重构.md`，保留面向维护者的模块接口和源码结构。

## 2026-06-01 卡片UI布局优化与全样式超速警示

- 电子眼摄像头图标新增限速数值叠加：经典 UI、Dashboard 和卡片 UI 的测速类摄像头会在图标上方显示红色限速数字，与灵动岛全功能模式逻辑一致。
- 卡片 UI 巡航布局重构：第一行固定 32dp、第二行固定 35dp，道路名称与方向文字交换位置，所有元素在行内垂直居中；巡航双行模式面板总高从 68dp 调整为 76dp。
- 卡片 UI 导航布局与巡航统一：导航面板总高固定为 76dp，转向图标从 36dp 放大到 44dp，距离文字从 13sp 放大到 16sp，道路名和 ETA 文字从 12sp 放大到 15sp，导航第一行固定 30dp、第二行固定 40dp。
- 修复卡片 UI 巡航第二行无内容时无法立即折叠为单行的问题，集群副屏同步增加行内容判断逻辑。
- 修复全样式悬浮窗宽度自适应问题：内容变宽后 `stabilizePanelSize` 会强制通知 WindowManager 更新窗口尺寸，避免多车道+红绿灯+电子狗等元素被挤出可视区域或错行显示。
- 新增全样式超速警示功能：当车速超过道路限速时，悬浮窗边缘显示 5dp 彩色边框并以 2Hz 频率闪烁；超过限速显示黄色、超过 10% 显示红色、超过 20% 显示深红色；车速回落至限速以下或限速消失后自动取消警示。
- 车速追踪从 `updateModeFromExtras` 提升到 `handleBroadcast` 公共层，所有 KEY_TYPE 的广播均可更新当前车速，巡航和导航模式均支持实时超速检测。
- 车速数据来源于高德广播的 `CUR_SPEED` / `SPEED` 字段，限速数据来源于 `LIMITED_SPEED` / `CAMERA_SPEED` 字段。

## 2026-05-31 灵动岛全功能模式

- 合入 [#13](https://github.com/zuo-qirun/amap-companion/pull/13) 的“灵动岛（全功能）”样式，新增导航转向信息与 ETA 轮换显示，保留旧 UI、灵动岛（紧凑）、灵动岛（测试）和新 UI。
- 导航状态下，全功能模式会在同一区域按 10 秒转向信息、5 秒 ETA 信息轮换显示；转向信息包含图标、剩余距离和待转入道路名，ETA 信息包含剩余距离和预计到达时间。
- 统一全功能模式主屏/副屏的信息列宽度，转向信息列和 ETA 列保持同等宽度，减少广播刷新时悬浮窗横向跳动。
- 道路名称改回系统原生跑马灯，并保留“文本不变不重新设置”的防重置逻辑，修复自绘跑马灯在信息列中来回快速闪动的问题。
- 修复全功能模式道路名可能不显示的问题：ETA 广播更新道路名后会同步刷新导航道路名，避免转向广播阶段还未拿到路名导致空白。
- 巡航状态下车头方向改为粘性显示：普通状态广播不再清空方向缓存，无新方向数据时保留上一帧，避免“车头：--”与真实方向来回闪动。
- 副屏/仪表同步支持全功能模式的转向、ETA、巡航道路名与车头方向显示。

## 2026-05-28 灵动岛紧凑模式

- 新增“灵动岛（紧凑）”悬浮窗样式，并保留原有 UI、灵动岛、新 UI 和副屏显示逻辑。
- 紧凑模式导航改为胶囊布局：左侧显示转向图标、剩余距离和待转入道路名，车道、红绿灯和电子眼在同一行紧凑展示。
- 紧凑模式巡航在道路名称下方新增“车头：方向”，方向会缓存最近有效值，避免高德重复广播时突然显示/消失。
- 紧凑模式车道信息仅在该模式下启用公共裁剪和 2dp 间隙，普通 UI、灵动岛和新 UI 的车道显示回滚到原仓库逻辑。
- 优化紧凑模式车道尺寸随悬浮窗缩放的比例，避免 100% 及放大后车道箭头过大。
- 紧凑模式红绿灯导航状态下将秒数放入灯圈内，巡航状态保持灯圈内显示方向箭头、下方显示倒计时。
- 电子眼提示改为复用固定子视图并延迟清除，减少公交道、压线、红绿灯等提示反复出现/消失；限速类电子眼会把限速值填入圆圈。
- 副屏/仪表的灵动岛紧凑布局同步主屏信息，补齐道路名、车头方向、车道、红绿灯和电子眼显示。

## 2026-05-26 亮屏自启与资源进一步精简

- “开机自动启动”改为“开机或亮屏自动启动服务”：开启后会在系统重启、应用更新、车机亮屏或解锁时自动启动伴侣服务，相当于打开软件后点击“启动伴侣服务”。
- 修正导航掉头红绿灯方向：掉头图标和右侧掉头图标不再被误判为右转；导航黄灯方向异常时会优先跟随当前导航转向，避免误显示掉头。
- 转向图标统一优先使用更美观的 `sou*_night_a530` 资源，并删除普通 `sou*_night`、不可达的 50+ 环岛出口资源和备用 `turn_*.png`。
- `sou` 转向资源由约 `269KB` 精简到约 `59KB`；删除 `turn_*.png` 后，当前签名 APK 体积约 `515KB`。
- 电子眼悬浮窗扩展支持更多高德 `CAMERA_TYPE`：按高德原始 Widget 图标显示监控、闯红灯、公交道、应急车道、非机动车道、压线、违停、鸣笛、逆行、HOV 等提示，不再只显示限速/摄像头/红绿灯三类。
- 优化导航红绿灯方向兼容：导航态 `dir=0` 不再直接视为掉头，黄灯 3 秒会延续上一阶段绿灯/红灯记录到的方向，避免黄灯尾秒误显示掉头箭头。

## 2026-05-24 手动融合 PR #9 与灵动岛优化

- 手动融合 [#9](https://github.com/zuo-qirun/amap-companion/pull/9) 的主要改动，并保留当前版本已有的副屏、日志、自定义字体、DIY 资源和高德广播自动显示逻辑。
- 新增“灵动岛（测试）”悬浮窗样式：巡航时采用更紧凑的胶囊布局，车道线间隔缩小，转向提示、车道和红绿灯会按优先级显示。
- 旧 UI / 新 UI / 灵动岛统一使用电子眼、限速、红绿灯图标；修正电子眼距离后偶发多出限速数字的问题，限速数字会优先填入限速圆圈。
- 蓝色转向框改为两行显示：第一行道路名称，第二行转向图标和剩余距离；导航退出时会同步清除蓝色框和残留图标。
- 修正刚进入导航或导航状态切换后，蓝色转向框可能只恢复箭头、道路名和剩余距离仍保持隐藏的问题。
- 优化悬浮窗整体宽度稳定性：内容短暂变少时不会立刻缩小边框，避免高德广播字段忽多忽少导致整个悬浮窗宽度来回抖动。
- 蓝色转向框和红绿灯方向箭头优先使用高德原始 `sou*_night` 转向资源，50+ 扩展动作码不再直接映射到环岛出口资源，避免“进入高速”等场景误显示环岛数字箭头。
- 根据高德 `GuideInfoProtocolData.switchIcon()` 逻辑补充 `65 -> 4`、`66 -> 5` 的扩展动作兼容映射，减少进入/靠左靠右类动作显示成默认直行的情况。
- 主屏导航红绿灯补充方向标识，和副屏/仪表显示保持一致；倒计时去除 `s` 后缀，灵动岛红绿灯尺寸略微收紧。
- 进一步缩小红绿灯胶囊、方向箭头/圆点和倒计时字号，减少红绿灯区域对悬浮窗宽度和高度的占用。
- 红绿灯胶囊背景改为纯色半透明背景，避免透明度调低后文字与地图白色背景混在一起。
- 修正 8 车道等宽车道场景下，副屏/仪表悬浮窗靠近屏幕边缘时被自动向左挤的问题。
- 车头方向不再显示角度，改为“北/东北/东/东南/南/西南/西/西北”等文字方向；ETA 区域优化为距离、时间、到达时间和目的地分行展示。

## 2026-05-24 进入软件自动启动服务

- 新增“进入软件后自动启动服务”选项；打开 AMap Companion 主界面后会按用户已选择的主屏、副屏或高德广播自动显示设置静默启动伴侣服务。
- 去除车道信息区域的黑色卡片背景，仅保留车道图形显示。
- 美化导航和巡航红绿灯样式；巡航方向灯改为更大的圆形箭头徽章加倒计时，导航单灯保留状态灯点加倒计时。
- 修正调整副屏/仪表百分比大小后，车道信息没有跟随缩放的问题。
- 新增多红绿灯巡航箭头 DIY：可在 `/sdcard/amap_companion/diy/` 放置 `cruise_arrow_left/right/straight/uturn` 的 PNG、WebP 或 JPG 图片，软件会等比适配到固定箭头位。
- 统一单红绿灯状态圆点与多红绿灯箭头圆圈的视觉尺寸。
- 红绿灯胶囊背景从颜色到透明渐变调整为纯色半透明背景，降低透明度时更适合贴在地图上显示。
- 日志公共保存目录调整为 `/sdcard/amap_companion/log`，与 DIY 资源目录统一放在 `/sdcard/amap_companion` 下。
- 修正副屏位置调整按钮偶发无效的问题；位置按钮现在同步保存坐标并通过专用广播即时移动副屏窗口，不再依赖重建副屏。
- 新增自定义字体功能：在 `/sdcard/amap_companion/` 或 `/sdcard/amap_companion/diy/` 放置 `font.ttf/font.otf/custom_font.ttf/custom_font.otf` 后，主界面、悬浮窗、副屏和红绿灯会自动使用该字体。

## 2026-05-23 资源轻量化

- 清理未被当前车道显示逻辑引用的旧版/备用车道图片资源，仅保留实际动态加载的 `lane_pdf_0..48` 和通知图标。
- `drawable` 资源从 `224,560B` 降至 `101,973B`，减少约 `54.6%`；整个 `app/src/main/res` 从 `516,103B` 降至 `393,516B`，减少约 `23.8%`。
- 当前本地签名 APK 体积为 `422,723B`，约 `412.8KB`，应用包更轻量。
- 更新安装流程新增备用通道：PackageInstaller 被车机拦截后，会继续尝试 InstallerX / InstallerX Revived，再使用带真实 `content://` APK URI 的系统默认安装器，减少小鹏车机 `uri:null` 安装拦截导致的“解析包时出现问题”。

## 2026-05-22 高德广播自动显示与日志调试优化

- 新增“高德广播自动显示悬浮窗”开关：开启后会通过前台服务在后台持续监听高德广播，收到高德地图广播后自动显示悬浮窗，不再依赖“使用情况访问权限”。
- 主界面“启动/关闭悬浮窗”改为“启动/关闭伴侣服务”，主屏悬浮窗和副屏悬浮窗改为独立显示位置选项；手动启动和高德广播自动显示都会跟随用户选择。
- 修正“关闭伴侣服务”在高德广播自动显示开启时无法真正停止的问题；关闭时会同时关闭主屏、副屏和广播自动显示监听。
- 日志页将 `READ_LOGS` 和存储权限拆分为两个授权入口；保存日志只根据 `/sdcard/amap_log` 是否可写决定是否使用公共目录，不再要求同时具备日志读取权限。
- 日志可在应用内查看、复制或保存；优先保存到 `/sdcard/amap_log`，无权限时回退到应用私有日志目录，方便反馈和排查问题。
- 去除车道区域的“车道信息”标题，只保留车道图形；悬浮窗和副屏最小缩放下限调整为 `30%`。

## 2026-05-23 副屏仪表稳定与通知弹窗修复

- Fix companion service stop behavior：伴侣服务关闭逻辑会同步停止主屏/副屏显示与高德广播自动显示监听，避免服务残留。
- 修正副屏悬浮窗在“导航/巡航退出隐藏仪表”开启时可能被高德打开阶段误隐藏的问题；高德广播自动显示触发后，副屏会跟随用户的副屏选项启动。
- 修正副屏悬浮窗反复开启/关闭时可能出现大小不一致的问题；副屏重建会强制重新读取当前“副屏大小”，启动伴侣服务也不再自动改动主屏显示选项。
- 修正切换主屏/副屏悬浮窗开关时可能触发前台服务启动超时并闪退的问题；关闭选项时不再先启动服务。
- 修正副屏面板尺寸计算混用主屏密度的问题；副屏 Presentation 构建和红绿灯胶囊现在固定使用副屏 Context 的密度，避免大小一会儿像主屏、一会儿像副屏。
- 修正副屏仪表面板在部分车机/投屏 display 上尺寸异常的问题：副屏面板会按仪表屏分辨率限制最大绘制比例，并避免新 UI 左右分栏在 1080p 仪表屏上重叠。
- 修正进入 AMap Companion 主界面时可能自动启动前台服务，导致系统弹出“是否允许发送通知”并临时拦截触摸的问题；现在只有用户点击“启动伴侣服务”、开机后台监听或收到对应广播时才启动服务。
- 安装/覆盖更新后不再通过 `MY_PACKAGE_REPLACED` 自动拉起前台服务，避免刚安装完成就触发通知权限弹窗；开机自动监听仍按用户开关保留。
- 软件内发现新版本弹窗改为可滚动 Markdown 渲染，支持标题、列表、代码块和行内反引号，更新日志阅读更清晰。
- 解除副屏仪表面板按 DPI/分辨率计算的最大缩放限制，“副屏大小”滑杆会按用户设置的百分比直接生效。
- “下载已改高德”入口更新到新的定制地图 Release，APK 文件名改为英文并保留镜像站下载选项。
